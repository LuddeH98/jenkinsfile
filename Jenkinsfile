def generateStage(bar) {
    agent {
        docker {
            image 'ubuntu:latest'
        }
    }
}

pipeline {
    agent any
    stages {
        stage('Main') {

            steps {
                sh "echo Building for aaa"
            }
        }
    }
}

