def generateStage(bar) {
    stage ("${bar}") {
        sh "echo Building for ${bar}"
    }
}

generateStage("bar")

pipeline {
    agent any
    stages {
        stage('Main') {
            steps {
                script {
                    
                }
            }
        }
    }
}

